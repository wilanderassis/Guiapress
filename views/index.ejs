<%- include ('partials/header.ejs') %>
    <%- include ('partials/homenavbar.ejs') %>

        <body>

            <div class="container">

                <hr>
                <div class="jumbotron">
                    <h2>Bem vindo ao meu blog!</h2>
                </div>
                <hr>

                <% articles.forEach((article)=>{ %>

                    <div class="card">
                        <div class="card-header">
                            <h2>
                                <%= article.title %>
                            </h2>
                        </div>
                        <div class="card-body">
                            <a href="/<%= article.slug %>" class="btn btn-success">Ler artigo</a>
                        </div>
                    </div>
                    <br>

                    <% }) %>

                        <div class="row mb-4" style="width: 100%; display: flex; justify-content: flex-end;">
                            <a href="/articles/page/2 %>">Próximo >></a>
                        </div>

            </div>

        </body>
        <%- include ('partials/footer.ejs') %>